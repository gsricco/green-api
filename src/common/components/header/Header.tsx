import React from 'react';
import styles from './Header.module.scss'

const Header = () => {
    return (
        <div className={styles.header}>
            <div>WhatsApp messaging App</div>
        </div>
    );
};

export default Header;